<div class="container container-fluid mh-100">
   <div class="row">
     <div class="col-lg-5">
       <div class="jumbotron h-100 text-center d-flex flex-column justify-content-center mt-3 landing-jumbo">
         <h1 class="display-4">Household Duties Tracker App</h1>
         <p class="lead"><%= t('landing.index.hello') %></p>
         <hr class="my-4">
         <p><%= t('landing.index.tip') %> </p>
         <%= link_to t('landing.index.login'), new_user_session_path, class: "btn btn-success btn-lg" %> <%= t('landing.index.or') %>
         <%= link_to t('landing.index.signup'), new_user_registration_path, class: "btn btn-info btn-lg" %>
       </div>
     </div>
   </div>
</div>
