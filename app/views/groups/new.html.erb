<div class="container">
  <div class="row">
    <div class="col-sm">
      <h2><%= t('groups.new.heading') %></h2>
      <%= simple_form_for(@group, url: groups_path, method: :post) do |f| %>
        <%= f.error_notification %>

        <%= render :partial => 'form', locals: {f: f} %>

        <div class="form-actions">
          <%= f.button :submit, t('groups.new.action') %>
          <%= link_to t('common.back'), groups_path, class: "btn btn-info btn-md" %>
        </div>

      <% end %>
    </div>
  </div>
</div>