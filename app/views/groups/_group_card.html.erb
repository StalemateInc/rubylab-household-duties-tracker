<div class="card">
  <div class="card-body">
    <h4 class="card-title"><%= group.name %></h4>
    <p class="card-text"><%= pluralize(group.memberships.count, "member") %></p>
    <%= link_to "More", group_path(group), class: "btn btn-md btn-success" %>
  </div>
</div>